name: HTTP Manager IT Tests

on:
  workflow_dispatch:

jobs:
  http-manager-it:
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libcmocka0 libcmocka-dev pkg-config

      - name: Build and run HTTP manager IT tests
        run: ./utils/tests/run.sh

      - name: Run HTTP manager timing tests
        run: ./utils/tests/run_timing.sh
